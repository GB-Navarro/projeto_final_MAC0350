```gherkin
Funcionalidade: Reestilização das telas existentes com o novo design system
Regra de negócio: a reestilização não pode alterar nenhum comportamento — só aparência.

  Cenário: suíte de testes não muda de resultado
    Dado o conjunto de testes que passa hoje na main
    Quando o design system é aplicado a todas as telas
    Então o mesmo conjunto de testes continua passando, sem nenhum teste alterado

  Cenário: JavaScript existente continua funcionando
    Dado o formulário de questão com o editor LaTeX e alternativas dinâmicas
    Quando os estilos são trocados
    Então adicionar/remover alternativa e o preview KaTeX continuam funcionando
    (os seletores usados por formulario.js e latex_preview.js não são renomeados)
```

Checklist manual (não automatizável — projeto não tem teste de UI):
- [ ] Cada uma das 9 telas foi aberta no navegador e confere com os tokens do design system
- [ ] Responsivo verificado em largura mobile (~375px) em pelo menos lista e formulário de questões
